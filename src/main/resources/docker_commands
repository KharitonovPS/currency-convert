#create image from dockerfile
sudo docker build -t bot-app .

#run app in docker image
sudo docker run bot-app

#set up CI/CD on GitHub actions with gradle

#connect to shell in docker container
docker exec -it <mycontainer id/name> bash